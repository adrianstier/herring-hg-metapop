# Agent Notes For This Repository

This file captures project-specific modeling decisions that future coding agents should read before editing model code or interpretation docs.

## Current Scientific Direction

The promoted baseline is now Stier-aligned before adding more biological complexity.

As of 2026-05-11, use `m1_stier_11` as the practical analysis baseline:

- Stan file: `inst/stan/herring_metapop_m1_stier_11.stan`
- Fit script: `Code/03_fit_m1_stier_11.R`
- Fit artifact: `Data/processed/m1_stier_11_fit.rds`
- LOO artifact: `Output/posteriors/loo_m1_stier_11.rds`
- Status file: `Output/diagnostics/latest_model_status.md`
- Exact re-LOO note: `Output/diagnostics/m1_stier_11_exact_reloo.md`

The original PSIS-LOO had one high Pareto-k point, but exact re-LOO resolved it:
held-out 1970 Naden Harbour changed total LOOIC from 1953.02 to 1953.08, with
a clean refit. Treat that LOO warning as resolved unless the model/data change.

As of 2026-05-09, the current population/driver synthesis is in
`docs/current-population-driver-findings.md`, with a compact checkpoint in
`docs/may-9-analysis-decision-summary.md` and a diagnostic map in
`docs/may-9-analysis-output-index.md`. For the shortest table of headline
numbers, use `Output/diagnostics/may9_headline_findings.md` or regenerate it
with `Code/07q_may9_headline_findings_table.R`.

As of 2026-05-10, the most compact decision control sheet is
`Output/diagnostics/may10_integrated_evidence_matrix.md`, regenerated by
`Code/07ag_integrated_evidence_matrix.R`. Read this before starting another
model branch. It separates high-confidence baseline/observation/fishing claims
from moderate or held mechanisms such as the existing PDO effect, spatial process covariance,
predators, density dependence, and age/size structure.

The literature-grounded parameter roadmap is
`docs/literature-parameter-roadmap.md`. It synthesizes the NotebookLM paper
query and local PDFs. Its main observation-calibration recommendation has now
been tested as `m1_stier_obs_hier`: section-specific observation error plus
surface-era extra positive-observation variance. That branch is sampler-clean
but held because positive-spawn calibration worsened relative to
`m1_stier_11` (aggregate log10 RMSE 0.64 versus 0.56) and PSIS was less stable
(max Pareto k 1.29). Do not promote it; use it as a negative result showing
that extra observation variance alone is not enough.

Cloud model-running setup is documented in
`docs/cloud-model-running-setup.md`, with helper scripts in `cloud/`. The
near-term path can use one EC2 instance per model/re-LOO job with S3 sync, but
the preferred scalable path is AWS Batch using `cloud/model-farm-manifest.csv`.
That manifest is intended to farm all major model families: observation,
site-growth, spatial, density, predator, time-varying, exact re-LOO, and smoke
tests.
Use `docs/full-analysis-model-farm-scope.md` for the iterative
run/collect/audit/rerun decision framework. Every branch should end as
`promoted`, `supporting_sensitivity`, `needs_rerun`, `archived_excluded`, or
`data_blocked`.
As of 2026-05-12, predator work has a new Stier-aligned process branch:
`m5_stier_predation_pressure`, with Stan file
`inst/stan/herring_metapop_m5_stier_predation_pressure.stan` and fit script
`Code/03_fit_m5_stier_predation_pressure.R`. It uses annual HG predation
pressure from the private `stier-lab/pacific-herring-predators` repo via
`Code/02c_integrate_hg_predator_repo_products.R`. This branch keeps ambiguous
zeros, two-era q, and no age/size structure. Treat it as the first defensible
predator model to run on AWS, not as a promoted result until audit/PPC/LOO
clear.
Use `docs/aws-codex-model-farm-lessons.md` as the living implementation log for
what worked and failed while connecting Codex to AWS; update it immediately
when new AWS setup issues are discovered.

As of 2026-05-15, the Doherty-style WCVI replication path has a data-readiness
control sheet rather than a new age-structured model branch:
`Code/02d_fetch_dfo_herring_assessment_sources.R`,
`Code/02e_extract_dfo_hg_assessment_tables.R`,
`Code/02f_extract_newer_dfo_public_pdfs.R`,
`Code/07bk_doherty_hg_data_readiness.R`,
`Code/07bl_doherty_replication_execution_status.R`, and
`docs/doherty-style-hg-data-acquisition.md`. The source map is
`docs/doherty-style-hg-source-provenance.md`; update it whenever a public PDF,
DFO-request result, local herring table, or predator source enters this
workflow. Public DFO stock-assessment pages and appendices are the first
acquisition path for HG catch, age composition, number/weight/length-at-age,
maturity, and test-fishery biology. A DFO request should ask for
machine-readable copies and metadata for those already-published assessment
inputs. Do not fit a full 11-section catch-at-age model or a Doherty-style
predator-removal Stan branch until those herring biological inputs and predator
selectivity assumptions are extracted and audited.
`docs/doherty-style-hg-replication-status.md` records the executed state: public
DFO Appendix B HG catch, spawn, number-at-age, weight-at-age, biosample, and
maturity tables have been provisionally extracted through 2017; current
DFO 2025/005 public summary tables have been extracted through 2024 for catch,
spawn, SCA parameters, recruitment, biomass/depletion, reference points, and
broad projected age composition. Exact annual 2018-2024 age/weight input
matrices, length-at-age tables, effective sample-size metadata, predator
age/size selectivity, and future predator scenarios remain unresolved.
Every new extracted table should preserve `source_document`, `source_table`,
`source_url`, `extraction_method`, and `extraction_notes`; local/private
sources should record local path, upstream catalog, owner/custodian, and
model-use status.
`Code/07bm_doherty_public_extract_qc.R` checks public extracts for row counts,
year ranges, and source-field completeness. `Code/07bn_hg_dfo_sca_external_comparison.R`
adds a talk-facing comparison among `m1_stier_11`, public DFO 2025/005 HG SCA
summaries, and predator demand. Use it only as scale/geography context; do not
interpret it as a likelihood comparison or validation residual.
`docs/dfo-hg-biological-input-request-packet.md` is the tracked follow-up ask
for exact machine-readable HG SCA/SISCAH biological inputs and metadata.
For the May 16 talk cycle, `Code/07bo_doherty_proxy_parameter_plan.R` writes
`Output/diagnostics/doherty_proxy_parameter_plan.md` and
`Output/figures/doherty_proxy_parameter_plan.pdf`. This is the explicit proxy
ledger: HG public DFO tables anchor catch, age-composition, weight-at-age,
maturity, and current-status context where available; WCVI/Doherty values may
only be shown as provisional analogues for missing length/size/selectivity or
model-structure pieces. Do not describe WCVI catch-at-age, size-at-age, or
predator selectivity as estimated Haida Gwaii parameters. Do not fit or promote
a Doherty-style HG catch-at-age predator-removal model until exact HG biological
inputs, effective sample sizes/preprocessing rules, and predator selectivity
assumptions are acquired and audited. The talk-safe wording is: "we have a
source-traceable Doherty-style bridge and proxy plan, not a completed HG
catch-at-age predator-removal analysis."

The spawn-index scale audit is in `Output/diagnostics/spawn_index_scale_audit.md`
and `Output/figures/spawn_index_scale_audit.pdf`. It shows that Stier's legacy
SHI and the maintained DFO `spawn_index_tonnes` series are strongly related but
not connected by one constant multiplier: the median SHI / tonnes ratio is
about 112, while section medians range from about 68 to 380. Do not copy
legacy numerical `q` values into the DFO-tonnes model.

The May 9 `m2_stier_site_growth`, `m1_stier_method_sensitivity`, and
`m1_stier_obs_hier`, and `m3_stier_distance` branches all finished and are
held, not promoted. All were sampler-clean, but none materially improved
positive-spawn calibration over `m1_stier_11`. The three-era method sensitivity
estimated q medians of 0.187 for surface, 0.623 for the mixed transition, and
0.278 for dive-dominant years, but its aggregate positive-spawn RMSE was 0.569
versus 0.565 for `m1_stier_11`. The observation hierarchy had RMSE 0.637 and
max Pareto k 1.29. The distance branch estimated a practical
process-correlation range near 144 km, but its positive-spawn RMSE was only
0.556. Exact re-LOO for the three high-k distance points completed with
corrected LOOIC about 1949.27, but one exact refit had 213 treedepth hits, so
the branch still should not be promoted without stronger calibration evidence.
Treat `m1_stier_11` as the promoted practical
baseline unless a new branch clears the observation-calibration gates.
The method-sensitivity readout is in
`Output/diagnostics/m1_stier_method_sensitivity_postfit.md`; the distance
readout is in `Output/diagnostics/m3_stier_distance_postfit.md`.

The latest non-Stan driver screens add these operational conclusions:

- `Output/diagnostics/model_branch_status_table.md`: compact model status for
  Monday; `m1_stier_11` is promoted, `m1_stier_obs_hier` is clean but held,
  `m3_stier_distance` is a clean spatial candidate but held, and `m5_v3` must
  not be used as predator evidence.
- `Output/diagnostics/aws_batch_model_farm_status.md`: local read-only AWS
  Batch status report. If it is marked stale, the `herring` AWS SSO token must
  be refreshed with `aws sso login --profile herring` before polling or syncing
  cloud results.
- `Output/diagnostics/positive_spawn_fit_caveat.md`: focused fit caveat for
  the promoted baseline; surface-era positive-spawn RMSE is 1.65 versus 1.02
  for SCUBA/dive, early-industrial RMSE is 2.06 versus 0.96 in the recent
  closure period, so do not over-read individual early surface-era magnitudes.
- `Output/diagnostics/promoted_baseline_evidence_package.md`: shortest current
  evidence package for the promoted `m1_stier_11` baseline, including model
  branch status, biomass, spawn/catch fit, section roles, caveats, and priority
  figure paths.
- `Output/diagnostics/section_action_matrix.md`: current section-level work
  plan. Cumshewa/Louscoone are the lead mechanism cases, Skidegate/Laskeek/
  Rennell are portfolio erosion cases, Juan Perez/Skincuttle are current
  biomass concentration cases, Port Louis/Englefield are recovery contrasts,
  and Tasu/Naden are uncertainty sensitivity only.
- `Output/diagnostics/lead_section_local_audit.md`: local survey/context audit
  for Cumshewa, Louscoone, Laskeek, and Skidegate. It uses processed annual
  spawn records and raw HG location records to keep mechanism claims grounded
  in survey coverage and location concentration.
- `Output/diagnostics/current_biomass_uncertainty_decomposition.md`: explains
  the current biomass interval; the all-11 median is useful, but Tasu/Naden
  drive about 92% of the upper 5% posterior tail, so pair all-11 biomass with
  the focal-9 sensitivity.
- `Output/diagnostics/stier_signal_persistence_summary.md`: focused answer to
  whether the original Stier et al. process/growth/portfolio signal persists in
  the 2025 update.
- `Output/diagnostics/driver_model_triage.md`: report observation-scale caveats
  and historical fishing pressure now; interpret the existing lagged-PDO effect
  as the cleanest regional climate signal; hold predators, substrate/timing,
  and complex density dependence as context.
- `Output/diagnostics/fishing_pressure_decomposition.md`: recent/early biomass
  is negatively associated with mean historical fishing fraction (rho about
  -0.66), but fishing alone explains only about a quarter of section variation.
- `Output/diagnostics/fishing_closure_response.md`: fishing fraction drops
  from about 10.6% during the roe fishery to 0% recently; recent biomass is
  about 1.52x the roe-fishery median, but occupied sections fall from 8 to 5
  and top-three biomass share remains about 84%.
- `Output/diagnostics/m1_stier_11_portfolio_metrics.md`: use the regenerated
  promoted-baseline portfolio figure, not the stale April figure; recent all-11
  synchrony is about 0.63 and Simpson effective sections about 3.26.
- `Output/diagnostics/pdo_climate_signal_screen.md`: lag-1 PDO has a negative
  association with next-year growth (rho about -0.49, detrended r about -0.42),
  while the baseline Stan PDO coefficient remains negative but uncertain.
- `Output/diagnostics/pdo_window_sensitivity.md`: PDO mean lag 0-1 is slightly
  stronger in the cheap screen, but lag-1 PDO remains competitive; do not
  launch a redundant PDO-only branch before Monday.
- `Output/diagnostics/density_dependence_screen.md`: no strong descriptive
  archipelago-level negative density signal; do not jump to complex DD.
- `Output/diagnostics/section_mechanism_typology.md`: Cumshewa and Louscoone
  are the clearest persistent-depletion-beyond-fishing sections; Tasu and Naden
  are sensitivity sections because of sparse coverage and uncertainty.
- `Output/diagnostics/survey_coverage_zero_ambiguity.md`: the model matrix has
  495 positive site-years, 19 zero-record site-years, and 311 missing /
  unsurveyed site-years; keep zero records and no-survey cells ambiguous in the
  promoted baseline.
- `Output/diagnostics/predator_data_feasibility_audit.md`: current predator
  covariates are regional, highly collinear, and strongly time-confounded
  (combined predator index versus year rho about 0.96); do not launch a predator
  Stan branch before a separate spatial exposure data product exists.
- `Output/diagnostics/predator_spatial_exposure_prototype.md`: first spatial
  exposure prototype from raw Haida Gwaii harbour seal and Steller sea lion
  locations/counts. It shows that section-level exposure covariates are
  feasible, but the current screen is still time-confounded and lacks humpback
  section exposure, so it is a data-product roadmap rather than predator-effect
  evidence.
- `Output/diagnostics/section_recovery_covariate_screen.md`: descriptive n=11
  screen combining recovery ratio, historical fishing pressure, survey
  coverage, positive-spawn fit caveats, timing/substrate shifts, and prototype
  predator exposure. It keeps historical fishing as the strongest section-level
  recovery axis and treats predator/timing/substrate as data-product targets.
- `Output/diagnostics/lead_section_location_transition.md`: raw HG
  location-level persistence/loss screen for Louscoone, Cumshewa, and Laskeek.
  Current read: Louscoone and Cumshewa recent raw signal is near 1% of
  roe-fishery signal; Laskeek is higher but still depleted and spatially
  reconfigured. Skidegate is not in that raw HG section extract, so use the
  processed/model section series for Skidegate-specific claims.
- `Output/diagnostics/lead_section_location_map.md`: geocoded companion to the
  raw location-transition screen. It maps the mappable subset of Louscoone,
  Cumshewa, and Laskeek raw locations for local audit targeting. It is not
  effort-adjusted absence evidence.
- `Output/diagnostics/lead_spawn_location_predator_proximity.md`: local
  predator-proximity audit linking geocoded lead-section spawn locations to
  post-2005 harbour seal and Steller sea lion sites. Current read: predator
  proximity can now be computed locally, Louscoone is high for Steller sea lion
  proximity/exposure, and lost locations are not clearly more exposed than
  persistent locations. Use this as a local audit target list, not causal
  predator evidence.
- `Output/diagnostics/lead_location_followup_targets.md` and
  `Output/figures/lead_location_followup_targets.pdf`: named local target list
  combining location loss/persistence, raw spawn method/substrate metadata,
  coordinates, and seal/sea-lion proximity. Current top targets include Traynor
  Creek, Louscoone Inlet East, Kilmington Point, Skindaskun Island, Atli Inlet,
  Cumshewa Inlet, and Conglomerate Point. Use it to focus survey-access,
  habitat/substrate, and local-exposure review. A lost-location label is not
  effort-adjusted absence evidence.
- `Output/diagnostics/covariate_readiness_registry.md`: current covariate
  readiness control sheet. It separates what is already in `m1_stier_11`
  (catch removals, lag-1 PDO, surface/SCUBA q, ambiguous zeros, 11 sections)
  from descriptive screens, prototype products, and held model ideas. Use it
  before proposing another Stan branch.

1. Treat zero spawn records as ambiguous unless there is explicit evidence that a complete survey was designed to establish absence.
   - Some site-years are not surveyed for governance/access reasons, including Haida preferences, not because biomass is low.
   - Therefore no survey effort, and ambiguous zero records, should not push biomass downward.
   - The current detection-aware / informative-zero models are useful sensitivity analyses, not the default biological interpretation.
2. Keep survey-method differences explicit.
   - Stier et al. separated surface and SCUBA survey catchability.
   - This repo has evidence for a transition/mixed period, so a three-era sensitivity is reasonable, but a Stier-replication branch should use the original two-era split.
3. Do not transfer numerical `log.q` values across data scales without checking units.
   - Stier used the spawn habitat index scale.
   - The current maintained data use DFO `spawn_index_tonnes`.
   - The proportional observation equation transfers; the absolute posterior scale of `log.q` does not.
4. Separate model fitting from figure/reporting focus for section count.
   - Stier fit the model to 11 Haida Gwaii subpopulations but focused interpretation on 9 data-rich focal subpopulations.
   - Preserve both 11-section and 9-focal reporting sensitivities.
5. Hold off on full size/age structure for now.
   - Stier and Okamoto support biomass-based section-level modeling here.
   - Age composition and weight-at-age are future regional covariates or stock-area cross-checks, not a full 11-section age-structured model in the current branch.

## Source Anchors

- `Literature/Stier_et_al_2020_Ecosphere_Portfolio_Erosion.pdf`
- `Code/legacy-2019/Model1_diagonal_equal.R`
- `docs/parameter-comparison-stier2020.md`
- `docs/high-quality-analysis-scope.md`
- `docs/analysis-issues-and-fixes.md`

## Implementation Caution

Do not silently overwrite `Data/processed/jags_model_inputs_v2.RData` to change zero treatment or section selection. Create explicit sensitivity inputs or fit scripts with clear names, for example:

- `m1_stier_11`: zeros ambiguous/missing, Stier-like survey q, 11 fitted sections.
- `m1_stier_9_report`: same fit summarized over the 9 focal sections; implemented as `Code/06e_m1_stier_9_focal_reporting.R`.
- `m1_detection_zero_sensitivity`: surveyed zeros treated as informative nondetections.

When reporting fits, state whether zeros are ambiguous/missing or informative nondetections. That choice changes the biological interpretation.

Also state the likelihood unit. Raw LOOIC is not directly comparable between
positive-only models like `m1_stier_11` and surveyed-cell/detection-aware models
like `m1_v4` or `m1_v5`.

## Next Model Roadmap

Build new branches from the `m1_stier_11` observation layer rather than
promoting older stale `v3`/`v5` branches directly. Keep zeros ambiguous and the
two-era survey `q` split unless a branch name explicitly says otherwise.

Recommended sequence:

1. Finish reporting sensitivities from the existing fit:
   - 11 fitted sections,
   - Stier 9 focal sections,
   - positive spawn fit,
   - catch fit,
   - portfolio metrics on posterior states.
2. Use the May 9 diagnostics to keep the next process branch focused:
   - recent biomass is partly rebounding but occupied sections remain low,
   - closure removed direct fishing mortality, so persistent depletion is not
     explained by ongoing catch,
   - recovery is highly uneven among sections,
   - recent biomass is concentrated: the top 3 sections carry about 84% of
     all-11 posterior biomass, and the Simpson effective section count is only
     about 3.3,
   - three focal sections remain below 20% of their 1951-1965 section baseline
     in the recent closure period: Skidegate, Louscoone, and Cumshewa,
   - historical fishing pressure is negatively associated with section recovery,
   - the existing lagged-PDO term is the most stable regional climate signal,
   - predator indices remain descriptive until simpler process structure is
     stable because they are highly time-trended and strongly correlated with
     each other,
   - predator data now have a feasibility audit; current seal/SSL data can
     support future spatial exposure work, but the humpback series is basin-wide
     and not a Haida Gwaii section-level exposure series,
   - predator spatial exposure now has a prototype for harbour seals and
     Steller sea lions; refine effort correction, interpolation rules, and
     biologically defensible distance kernels before any predator Stan branch,
   - the combined section recovery covariate screen still ranks historical
     fishing pressure as the cleanest section-level axis; use predator,
     timing, and substrate screens to guide data work, not causal claims,
   - raw DFO `Method` labels are mixed after 1988, so the Stier two-era `q`
     split is useful but should not be described as a clean raw surface-to-dive
     method transition,
   - residual spawn-fit correlations decline weakly with effective distance;
     the direct distance-covariance branch is now tested; exact re-LOO
     completed but one exact refit had treedepth pressure, and the branch is
     held because it did not materially improve positive-spawn calibration,
   - density-dependence evidence is weak in posterior-median screens; if tested
     later, start with one global term only,
   - Cumshewa and Louscoone are the cleanest sections for mechanistic scrutiny
     beyond fishing; Tasu and Naden should be framed as sparse-data
     sensitivities.
3. Run observation/data-scale sensitivities:
   - completed/held: two-era `q` versus three-era surface/mixed/dive `q`,
   - completed/held: section-specific observation error plus surface-era extra
     variance in `m1_stier_obs_hier`,
   - current `spawn_index_tonnes` versus legacy SHI scale if it can be restored cleanly,
   - detection-aware zeros only as a sensitivity bracket.
4. Add process structure one layer at a time on the Stier observation layer:
   - `m2_stier_site_growth` is held; simple site productivity did not help,
   - site-specific process variance only after productivity is stable,
   - `m3_stier_distance` is completed/held; use its distance range as
     descriptive support, not as a promoted inference model yet,
   - global density dependence only if exact re-LOO or a simpler calibration
     branch justifies moving beyond `m1_stier_11`; do not start with
     section-specific DD,
   - timing/substrate covariates.
5. Do not launch a redundant PDO-only branch because `m1_stier_11` already
   includes lagged PDO. If more climate work is needed, test PDO lag/window
   sensitivity before predator covariates because PDO is less time-confounded.
6. Revisit predator covariates only after the process branch is sampler-clean and
   the positive spawn/catch fits remain calibrated.
7. Keep full age/size structure out of the section-level model for now. Use age
   and weight-at-age later as regional covariates or cross-checks, not as the
   next Stan state dimension.
