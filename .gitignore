# R
.Rhistory
.RData
.Rproj.user/
*.RData
*.rds

# renv
renv/library/
renv/staging/
renv/cellar/

# Large model output (regenerable)
Output/posteriors/*.RData
Output/posteriors/*.rds
Output/posteriors/*.csv
# Generated diagnostics (regenerable; ignored by default).
# EXCEPTION (force-tracked via `git add -f` to allow reproducibility checking
# without re-running the full pipeline):
#   - Output/diagnostics/reversibility_*.{md,csv}  (reversibility analysis outputs)
Output/diagnostics/
Output/*.csv
Output/*.png
*_output.txt
*_watch.log
*.log

# Legacy posteriors (diagnostic PDFs, regenerable)
Code/legacy-2019/Posteriors/

# Generated figures (regenerable; ignored by default).
# EXCEPTION (force-tracked via `git add -f` to allow reproducibility checking
# without re-running the full pipeline):
#   - Output/figures/lecture/deck/*.png   (lecture deck artifacts)
#   - Output/figures/reversibility_*.{pdf,png}  (reversibility paper figures)
#   - Output/figures/legends/reversibility_*.md  (reversibility figure legend text)
Output/figures/
Output/figures/*.png
Output/figures/*.pdf
Output/*.pdf

# Generated processed data products (force-add if a processed artifact is intended
# to become a maintained repository input)
Data/processed/*.csv
Data/processed/predators/
Data/raw/HG_biological_samples*.csv

# Literature PDFs (too large for git, stored on Google Drive)
Literature/
!Literature/.gitkeep

# OS
.DS_Store
Thumbs.db

# Stan
*.exe
*.o
*.so
*.dll

# Temporary
_temp*.pptx
__pycache__/
cloud/aws_batch_runs/
cloud/aws_results/
cloud/job_status/
cloud/logs/

# USUK Forum 2026 talk workspace (SEPARATE from core analysis).
# Moved 2026-05-20 from talk-usuk-forum-2026/ to analysis/04_talks/2026-royalsociety/.
# Track structure + working text/HTML docs; ignore heavy PDFs and private travel
# logistics, which live on disk + Google Drive.
analysis/04_talks/2026-royalsociety/**/*.pdf
analysis/04_talks/2026-royalsociety/**/*.pdf.imgzip.bak
analysis/04_talks/2026-royalsociety/**/*.pdf.preocr.bak
analysis/04_talks/2026-royalsociety/**/*.zip
analysis/04_talks/2026-royalsociety/Trip_Dossier/
# Attendee dossier: built from the CONFIDENTIAL "do not circulate" forum
# participant list — personal prep only, must never be committed/published.
analysis/04_talks/2026-royalsociety/Attendee_Dossier/

# Royal Society deck build (heavy/regenerable — sources are tracked)
analysis/04_talks/2026-royalsociety/**/*.pptx
analysis/04_talks/2026-royalsociety/Talk_Materials/deck_build/photos/
analysis/04_talks/2026-royalsociety/Talk_Materials/deck_build/slides_html/
analysis/04_talks/2026-royalsociety/Talk_Materials/deck_build/qa-*.jpg
analysis/04_talks/2026-royalsociety/Talk_Materials/deck_assets/

# --- cleanup 2026-05-18: regenerable / local-only ---
inst/stan/*.hpp
logs/

# Brainstorming visual companion
.superpowers/

# deck_build: deps + scratch QA screenshots (never commit)
analysis/04_talks/2026-royalsociety/Talk_Materials/deck_build/node_modules/
analysis/04_talks/2026-royalsociety/Talk_Materials/deck_build/q-*.jpg
analysis/04_talks/2026-royalsociety/Talk_Materials/deck_build/qx-*.jpg

# isolated dev worktrees
.worktrees/

# Stray testthat graphics stubs (generated by Rplots device when no real device
# is open during test runs; content-free and regenerable)
tests/testthat/Rplots*.pdf

# --- 2026-05-20 repo reorganization ---
# Cold-storage archive — track only small text indexes, ignore heavy binaries
_archive/**/*.pptx
_archive/**/*.bak
_archive/**/*.mp4
_archive/**/*.mov
_archive/**/*.png
_archive/**/*.pdf
_archive/**/*.zip

# Stale snapshots awaiting confirmed deletion
analysis/04_talks/**/Talk_Materials/_stale/

# Per-workstream analysis outputs (regenerable) — but keep the .gitkeep so the dir tracks
analysis/*/output/*
!analysis/*/output/.gitkeep

# Bioeconomics sub-package internals (after Phase E move)
analysis/03_bioeconomics/Output/
analysis/03_bioeconomics/renv/library/
analysis/03_bioeconomics/renv/staging/
analysis/03_bioeconomics/renv/cellar/
